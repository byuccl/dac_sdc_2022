name: Build and deploy Jekyll site to GitHub Pages

on:
  push:
    branches: *

jobs:
  link-checker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Check links
        run: make check
        